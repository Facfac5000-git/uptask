extends layout
include mixins/formProject

block content 
    .contenido-principal
        h1 #{pageName}

        if errors
            each error in errors 
                .alerta.error= error.message

        +formProject(project)
        